# TODO: Standard python import of constituent modules here
# Then use discover_plugins() to dynamically scan the _external_ plugins directory
# For now, this acts as a test for discover_plugins() though